# include "sort.h"/*** int_swap - Swap two integers in an array.* @ int_1: The first integer to swap.* @ int_2: The second integer to swap.**/void int_swap(int * int_1, int * int_2){int temptemp = *int_1* int_1 = *int_2* int_2 = temp}/*** bubble_sort - Sorts an array of integers in ascending order using the Bubble sort algorit.* @ array: Pointer to the array to be sorted.* @ size: Size of the array.**/void bubble_sort(int * array, size_t size){size_t isize_t jbool swappedi = 0j = 0if (array == NULL | | size < 2)returnfor (i=0i < (size - 1)i++){swapped = falsefor (j=0j < (size - i - 1)j++){if (array[j] > array[j + 1]){int_swap(& array[j], & array[j + 1])swapped = trueprint_array(array, size)}}if (swapped == false)break}}